<%= simple_form_for :search, url: pages_search_path, method: :get do |f| %>
  <div class="d-flex">
    <div class="p-3">
      <h5>Busque pelo serviço: </h5>
    </div>
    <div class="flex-grow-1">
      <div class="search-form-control form-group">
        <select class="form-control string required" name="search[query]" id="search_query">
          <option value="Churrasco">Churrasco</option>
          <option value="Compras">Compras</option>
          <option value="Limpeza de grelhas e espetos">Limpeza de grelhas e espetos</option>
          <option value="Toca pagode">Toca pagode</option>
        </select>
      </div>
    </div>
      <div class="p-2">
        <button name="button" type="submit" class="btn btn-ghost">
          <i class="fas fa-search"></i> Buscar
        </button>
      </div>
  </div>
<% end %>
